package ie.lero.spare.forensics.assessment.brs.parser;

public enum BigraphChangeOperation {

	ADD ("Add"), REMOVE ("Remove"), CLOSE_CONNECTION ("Close-Connection");
	
	BigraphChangeOperation (String type) {
		
	}
}
